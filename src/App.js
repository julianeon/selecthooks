import React from 'react';
import './App.css';
import SelectForm from './SelectForm';

{/*    background-image: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)), url("lizzo.png"); */}

function App() {
  return (
      <div>
      <SelectForm/>
    </div>
  );
}

export default App;
